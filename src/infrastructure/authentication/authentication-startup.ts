import './interceptors/inject-auth-token-interceptor';
import './interceptors/handle-auth-token-expiration-interceptor';

export default {
  init: () => {
    // Nothing to do - only imports are needed
  },
};
